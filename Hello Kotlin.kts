fun main () {
    val name = "Gusbim"
    val hobi = "Gaming"

    print("Halo nama saya   :")
    println(name)
    print("Hobi saya adalah :")
    println(hobi)
    print(if (true)"always true" else "always false")

}